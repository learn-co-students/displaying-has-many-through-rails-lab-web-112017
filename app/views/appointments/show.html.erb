<h1>Appointment</h1>

<h2> <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M" ) %> </h2>

<p> <%=link_to @appointment.doctor.name, doctor_path(@appointment.doctor)%> | <%=link_to @appointment.patient.name, patient_path(@appointment.patient)%> </p>
