<h1><%= @doctor.name %></h1>

  <p>Department: <%=@doctor.department %></p>
  <p>Appointments:</p>
  <ul><% @doctor.appointments.each do |appointment| %>
      <li><%= appointment.convert_to_string %> | <%= link_to appointment.patient.name, appointment.patient%></li>

<% end %></ul>
