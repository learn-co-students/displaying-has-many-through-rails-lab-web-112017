<h1><%= @patient.name %></h1>

<ul>
  <% @patient.appointments.each do |appt| %>
  <li>Date and Time:<%= appt.formatted_date %> with Dr. <%= link_to appt.doctor.name, doctor_path(appt.doctor) %>: </li>
  <% end %>
</ul>
